# good
升序：先定义一个数组，然后写一个函数，内容即为冒泡排序，外层循环是需要排序的数字一共9个，内层循环为单个数字所需要比较的次数，如果前面比后面的数大则交换位置，再在主函数中将数字输入并调用函数输出结果
结构体：先创建一个名为“student”的结构体，里面包含姓名 id 分数 然后创建函数分别是用指针访问输入与输出，在主函数动态创建一个结构体指针让其有正确的访问方向，防止野指针出现，访问前面的函数，然后再释放内存。
类：创建一个Car类，私有成员为颜色与号码，公有成员为构造函数以及析构函数，构造函数把颜色与号码初始化，析构函数负责清理，然后再用一个输出的函数，在主函数中创建两个具体车辆，并输出
在Ubuntu中的则是利用共享文件夹来将Windows系统的压缩包传入Ubuntu系统，然后用Ubuntu来查找对应文件地址并运行起来。
